document.addEventListener('DOMContentLoaded', function() {
    // Inicializa o carrossel
    $('.carrossel').slick({
        dots: true,
        autoplay: true,
        autoplaySpeed: 3000
    });
});